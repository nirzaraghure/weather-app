# System Architecture

```mermaid
graph TD
    A[.github] --> B[.github/workflows]
    B --> C[static-deploy.yml]
    D[ARCHITECTURE.md] --> E[Repository Documentation]
    F[client] --> G[client/.gitignore]
    G --> H[client/README.md]
    H --> I[client/package-lock.json]
    I --> J[client/package.json]
    K[client/public] --> L[index.html]
    L --> M[manifest.json]
    M --> N[robots.txt]
    O[client/src] --> P[App.css]
    P --> Q[App.js]
    Q --> R[App.test.js]
    S[client/src] --> T[index.css]
    T --> U[index.js]
    V[client/src] --> W[reportWebVitals.js]
    X[client/src] --> Y[setupTests.js]
    Z[server] --> AA[.env]
    AA --> BB[index.js]
    BB --> CC[node_modules]
    CC --> DD[package-lock.json]
    DD --> EE[package.json]
    F --> O
    O --> K
    K --> L
    L --> M
    M --> N
    F --> Z
    Z --> AA
    AA --> BB
    BB --> CC
    CC --> DD
    DD --> EE

This Mermaid diagram represents the repository structure, showing the relationships between folders and files. The main components are:

- `.github`: contains workflow files and repository documentation
- `client`: the frontend application, including static files and source code
- `server`: the backend application, including configuration files and source code
- `public`: contains static files for the frontend application
- `src`: contains source code for the frontend application
- `node_modules`: contains dependencies for the backend application
- `package-lock.json` and `package.json`: contain dependency information for the frontend and backend applications
```

> Generated by GitGenius Sentinel